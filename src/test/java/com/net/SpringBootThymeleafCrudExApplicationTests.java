package com.net;


import org.springframework.boot.test.context.SpringBootTest;

//@RunWith(SpringRunner.class)
@SpringBootTest
public class SpringBootThymeleafCrudExApplicationTests {

	/*
	 * @Autowired private StudentService studentService;
	 * 
	 * @MockBean private StudentRepository studentRepository;
	 * 
	 * @Test public void saveTest() { Student student = new Student(1, "siva",
	 * "gpl"); when(studentRepository.save(student)).thenReturn(student); //
	 * assertEquals(student, studentService.saveStudent(student)); }
	 * 
	 * @Test public void deleteTest() { Student student = new Student(5, "siva",
	 * "gpl"); studentService.deleteStudentById(student); verify(studentRepository,
	 * times(1)).delete(student); }
	 */
}
